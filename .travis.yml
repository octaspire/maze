language: c

compiler:
    - clang
    - gcc

install:
    - sudo apt-get install libsdl2-dev libsdl2-image-dev

before_script:
    - mkdir build
    - cd build
    - cmake ..

script:
    - make
    - test/octaspire-maze-test-runner

